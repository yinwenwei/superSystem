<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.srqy.supersystem.manager.organ.mapper.OrganMapper">

    <select id="findAll" resultType="organ" parameterType="organ">
        select * from t_organ
        order by parentId,seq desc
    </select>

</mapper>